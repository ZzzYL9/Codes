该程序实现了一个简单的sql
将MySQL重新封装
通过解析自己的输入的语句操作数据库

并且在创表同时创建一个对应表的TXT文件
在对数据库进行插入与删除时会同时对TXT文件进行修改

编译文件命令
    g++ -c my_sql.cpp
    g++ -c main.cpp
    g++ main.o my_sql.o -omy_sql -L /usr/lib/mysql -lmysqlclient
